using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NPC : MonoBehaviour {
	//Für nächstes Mal: NPC Dinge aus dem Interactable hier rein


	// Start is called before the first frame update
	void Start() {
		
	}

	// Update is called once per frame
	void Update() {

	}

	//Interpolation
	//Mathf.Lerp
	//Vector3.Lerp
	//Wert A, Wert B, Prozentzahl T
	//Wenn T: 0, Ergebnis = A
	//Wenn T: 1, Ergebnsi = B
}
